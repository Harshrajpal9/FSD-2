//write a express js script to pass data like msg, name and id from express application to pug template in h1,h2,h3
// tag respectively and display data in browser
html 
head 
title 
body 
h3 #{message}
h3 #{name}
h3(style="color:red") #{id}
